<!DOCTYPE html>
<html class="nation-budget">
  <head>
    <meta charset="utf-8">
    <title>NationBudget</title>
    <base href="http://fast-brook-4391.herokuapp.com/">
    <link rel="stylesheet" type="text/css" href="static/assets/stylesheets/css/main.css">
  </head>
  <body class="register-personal-page">
    <header>
      <div>
        <a class="company-logo" href="/">NationBudget</a>
      </div>
      <div class="h1-container">
        <h1>Personal Plan</h1>
      </div>
      <div></div>
    </header>
    <section>
      <div class="main-container">
        <form action="getstarted/personal"
              method="post">
          <div>
            <input type="text"
                   placeholder="First Name"
                   name="firstName"
                   maxlength="50"
                   pattern="[a-zA-Z0-9-]+"
                   title="Name can only include letters, numbers, and hyphens"
                   required>
          </div>
          <div>
            <input type="text"
                   placeholder="Last Name"
                   name="lastName"
                   maxlength="50"
                   pattern="[a-zA-Z0-9-]+"
                   title="Name can only include letters, numbers, and hyphens"
                   required>
          </div>
          <div>
            <input type="email"
                   placeholder="Email"
                   name="email"
                   maxlength="256"
                   required>
          </div>
          <div>
            <input type="password"
                   placeholder="Password"
                   name="password"
                   pattern="(?=.*\d)(?=.*[a-z])(?=.*[A-Z]).{8,}"
                   title="Must contain at least one number and one uppercase and lowercase letter, and at least 8 or more characters"
                   required>
          </div>
          <div>
            <input type="submit"
                   class="register-button button"
                   value="Register!">
          </div>
        </form>
      </div>
    </section>
    <footer>
      <p class="flex-center">&#169;2015 NationBudget</p>
    </footer>
  </body>
</html>
